<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="XerSize.Views.Components.RoutineDropdown"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="This">

    <Border Style="{StaticResource CardBorderStyle}">
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
            <Picker
                Title="Select routine"
                ItemsSource="{Binding Source={x:Reference This}, Path=ItemsSource}"
                ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding Source={x:Reference This}, Path=SelectedItem, Mode=TwoWay}" />

            <Button
                Grid.Column="1"
                Text="Manage"
                Style="{StaticResource SecondaryButtonStyle}"
                Command="{Binding Source={x:Reference This}, Path=ManageCommand}" />
        </Grid>
    </Border>
</ContentView>